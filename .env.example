# Server configuration — copy to `.env` and fill in real values.
# JWT_SECRET is REQUIRED: the server crashes on boot if missing or empty (fail-fast, no default).
JWT_SECRET=
NODE_ENV=development
PORT=3000
